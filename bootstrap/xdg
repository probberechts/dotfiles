#!/usr/bin/env bash
#
# Create XDG dirs
#

set -e

if [[ -z "$DOTFILES" ]]; then
  cd -- "$(dirname "$0")/.." || exit 1
  DOTFILES="$PWD"
fi
. "${DOTFILES}/lib/helpers.sh"
. "${DOTFILES}/lib/pretty.bash"

[[ $DKO_SOURCE == *"shell/vars"* ]] || {
  echo "Must source shell/vars.sh first"
  exit 1
}

mkdir -p -- "$XDG_STATE_HOME"
[[ "$FONTCONFIG_PATH" != "" ]] && mkdir -p -- "$FONTCONFIG_PATH"
